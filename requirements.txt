# ============================================================
# 智能财务分析平台 V8.0 — Python 依赖（实测锁定）
#
# 更新：2026-07-10，Agent 全量15题评测后锁定
# 关键约束：
#   - numpy 2.x + scipy 1.18 配套（scipy 1.13 DLL 不兼容 numpy 2.x）
#   - langchain-core 1.4.9 运行时兼容 langchain 0.3.7（pip 元数据不冲突）
#   - langgraph 0.2.76 是最后一个兼容此组合的版本
# ============================================================

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# LLM & RAG 框架
langchain==0.3.7
langchain-core==1.4.9          # 需 >=1.x 以兼容 langgraph jsonplus.py
langchain-chroma==0.2.6
langchain-community==0.3.7
langchain-text-splitters==0.3.8
langchain-huggingface==1.2.2

# AI API（DeepSeek 兼容 OpenAI SDK）
openai==2.45.0

# 向量数据库
chromadb==1.5.9

# 文档解析
pymupdf==1.25.0
python-docx==1.1.0
markdown==3.7

# 本地 Embedding（BAAI/bge-base-zh-v1.5, 768维）
sentence-transformers==5.6.0
numpy==2.2.4                  # 必须 2.x（scipy 1.18 强制要求）
scipy==1.18.0

# 中文分词（BM25 检索 + 138 财务术语词典）
jieba==0.42

# 工具
python-dotenv==1.0.0
pydantic==2.10.0
pydantic-settings==2.7.0
loguru==0.7.0

# 业务数据库（SQLite → 生产切 MySQL）
sqlalchemy==2.0.51

# Agent 框架
langgraph==0.2.76             # 最后兼容此组合的版本
langchain-openai==1.3.4
tenacity==9.1.4

# 图表生成
matplotlib==3.9.0

# 测试
pytest==9.0.0
pytest-cov==7.0.0
rank-bm25==0.2.0          # BM25 关键词检索

# 外部数据源
akshare==1.18.0           # A股实时行情+财务报表（新浪财经）

# 分布式缓存/限流
redis==5.0.0
