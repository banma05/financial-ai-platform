# V8.1 质量打磨计划

> 审查日期：2026-07-10
> 审查方式：code-review (安全/边界/错误处理/性能/API契约) + 八荣八耻自检
> 测试基线：后端 385/385 ✅ | 前端 14/14 ✅ | SQL 95.8% | Agent 77.8% | RAG 100%

---

## 🔴 高优先级（生产前必须修）

| # | 问题 | 文件:行 | 风险 |
|---|------|------|:--:|
| D1 | **OpenAI 客户端无超时** — LLM 调用无响应时永久阻塞 worker | `model_router.py:101` | 生产宕机 |
| D2 | **CORS 默认源不包含 :5173** — 新前端无法跨域访问后端 | `main.py:28` | 开发环境 403 |
| D3 | **async def 路由内同步阻塞 LLM** — 3并发用户=2个被阻塞 | `api/rag.py:248-266` | 并发瘫痪 |
| D4 | **开发模式零鉴权** — API_KEY 为空时所有端点对外开放 | `auth.py:127` | 数据泄露 |
| D5 | **会话存储无限增长无 TTL** — 内存 `_session_store` 永不清理 | `api/rag.py:35` | 内存泄漏 |
| D6 | **API 路由直接操作 ORM** — 无 `get_db()` 依赖注入 | `api/rag.py` 多处 | 不可测试 |

## 🟡 中优先级（影响质量但不阻塞）

| # | 问题 | 文件:行 | 说明 |
|---|------|------|------|
| D7 | ECharts 加载但从未使用（base64 路径覆盖） | `ReportView.tsx:1,95` | 约 120KB 无用代码 |
| D8 | 5 处 `as unknown as` 类型断言 | PresetAnalysis/DocumentUpload | API 改结构时编译期不报错 |
| D9 | 三套会话存储竞争 | rag.py / redis_client.py | 无单一事实来源 |
| D10 | SSE 流式端点前端未使用 | agent.py:46 / rag.py:269 | 或实现前端 SSE 或删除代码 |
| D11 | 熔断器已实现但未接入 | `retry.py:155` | 频繁失败不会快速失败 |
| D12 | MIME `application/octet-stream` 被允许上传 | `api/rag.py:113` | 降低文件上传绕过门槛 |
| D13 | `eval()` 计算财务公式 | `financial_query.py:235` | 低风险但应替换 |
| D14 | BM25 索引用 `chroma.get()` 加载全部文档 | `hybrid_search.py:80` | 万级 chunk 时内存瓶颈 |

## 🟢 低优先级（锦上添花）

| # | 问题 |
|---|------|
| D15 | `requirements.txt` 中 4 个包未使用（pydantic-settings 等） |
| D16 | `tslib` 在 `package.json` 中但 esbuild 不需要 |
| D17 | 前端公司列表硬编码，需与后端同步 |
| D18 | 硬编码 "2024年" 将随时间过期 |
| D19 | 数据库路径/后端端口/Vite 代理 URL 硬编码 |
| D20 | CI 补全（前端测试 + GPU 测试覆盖率） |
| D21 | 评测报告 `evaluation/reports/` 应加入 `.gitignore` |

---

## 📋 已知限制（不修，仅记录）

| # | 限制 | 原因 |
|---|------|------|
| L1 | 口语化表达 "赚了多少钱" 不支持 | SQL 查询引擎不处理口语化 |
| L2 | AKShare 数据精度差异（S16 茅台资产负债率 12.55→16.42） | 数据源问题，非代码 |
| L3 | Agent 评测 3/15 超时（>30s） | LLM 延迟不可控 |
| L4 | pyarrow 段错误（需要正确 import 顺序） | Windows DLL 加载顺序问题 |
| L5 | 前端无 SSR/预渲染 | 设计选择（SPA） |

---

## 🗓️ 建议实施顺序

| 阶段 | 事项 | 预计 |
|------|------|:--:|
| **D-A** 安全防线 | D1 超时 + D2 CORS + D4 鉴权 + D12 MIME | 30min |
| **D-B** 并发稳定 | D3 async 阻塞 + D5 会话泄漏 + D14 BM25内存 | 45min |
| **D-C** 架构清理 | D6 ORM 注入 + D9 会话统一 + D13 eval替换 | 1h |
| **D-D** 质量收尾 | D7 ECharts + D8 类型 + D15-D21 剩余 | 1h |
